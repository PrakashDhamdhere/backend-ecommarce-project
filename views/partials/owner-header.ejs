<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="">
    <% let loggedin = typeof(loginPage) == "undefined" ? true : false %>
    <% if(loggedin) {%>
    <nav class="w-full h-[10vh] bg-zinc-200 flex items-center justify-between px-10">
        <h1 class="font-bold text-2xl text-zinc-700">Admin Panel</h1>
        <div class="flex items-center gap-5">
            <a href="/" class="bg-green-500 px-2 py-1 rounded-md text-white font-semibold">Website</a>
            <a href="/owners/logout" class="bg-red-500 px-2 py-1 rounded-md text-white font-semibold">Logout Admin</a>
        </div>
    </nav>
    <%}%>